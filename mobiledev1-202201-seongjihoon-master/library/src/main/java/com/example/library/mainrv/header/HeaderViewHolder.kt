package com.example.library.mainrv.header

import androidx.recyclerview.widget.RecyclerView

class HeaderViewHolder(itemHeader: ItemHeader) : RecyclerView.ViewHolder(itemHeader) {
    fun bindWithView() {
        //bind
    }
}
